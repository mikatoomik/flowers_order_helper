  <%= simple_form_for(thing)  do |t| %>
      <%= t.input :name %>
      <%= t.input :color if thing == @flower %>
      <%= t.input :date, html5: true if thing == @order %>
      <%= t.submit %>
  <% end %>
